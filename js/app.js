import { initRouter } from './router.js';

// Inicia a aplicação assim que o HTML estiver carregado
document.addEventListener('DOMContentLoaded', () => {
  console.log('🚀 Aplicação iniciada');
  initRouter();
});
